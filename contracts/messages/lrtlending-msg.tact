struct DepositNFT {
    queryId: Int;
    nftId: Int;
}

struct ReturnNXT {
    queryId: Int;
    amount: Int;
}

struct TransferNXT {
    queryId: Int;
    amount: Int;
}

struct TransferNFT {
    queryId: Int;
    nftId: Int;
}
