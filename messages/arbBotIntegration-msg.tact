struct TransferUSDT {
    queryId: Int;
    amount: Int;
}

struct SignalExternalWallet {
    queryId: Int;
    nftId: Int;
    amount: Int;
}

struct UpdateYieldRate {
    queryId: Int;
    newRate: Int;
}
